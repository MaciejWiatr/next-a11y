# next-a11y Demo

```bash
npx next-a11y scan examples/demo          # BEFORE: 18 fixable
npx next-a11y scan examples/demo --fix   # AFTER: 0 fixable
```

- `FeaturedProduct.tsx` — main demo component
- `icons.tsx` — icon components (Cart, Heart, Share, Trash, etc.)
